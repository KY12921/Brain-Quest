name = "brain-quest-api"
main = "src/index.js"
compatibility_date = "2026-01-01"
compatibility_flags = ["nodejs_compat"]

# Non-secret config. Secrets (API keys, service account JSON) are set
# separately via `wrangler secret put NAME` — see the README's
# "Connecting the real AI Tutor" and "Taking real payments" sections.
[vars]
FIREBASE_PROJECT_ID = "study-boss-3e3e4"
